{
  "name": "eatsha",
  "version": "1.0.2",
  "private": true,
  "packageManager": "bun@1.3.14",
  "scripts": {
    "build": "bun scripts/write-version.js && mkdir -p dist && cp index.html styles.css app.js version.js favicon.svg _headers dist/",
    "deploy": "bun run build && bunx wrangler pages deploy dist --project-name=eatsha",
    "release": "bun scripts/release.js"
  }
}
